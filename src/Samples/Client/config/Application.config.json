{
  "dev": {
    "routerUri": "inproc://router",
    "scaleOutAddressUri": "tcp://*:5201",
    "pingSilenceBeforeRendezvousFailover": "00:00:20",
    "pongSilenceBeforeRouteDeletion": "00:00:10",
    "runAsStandalone":  false,

    "rendezvousServers": [
        {
            "broadcastUri": "tcp://localhost:5001",
            "unicastUri": "tcp://localhost:5011"
        },
        {
            "broadcastUri": "tcp://localhost:5002",
            "unicastUri": "tcp://localhost:5012"
        },
        {
            "broadcastUri": "tcp://localhost:5003",
            "unicastUri": "tcp://localhost:5013"
        }
    ]
  }
}
